package com.aegis.safety.core.location

// LocationProvider is bound directly via @Inject constructor + @ApplicationContext.
// No provider module is needed. This file intentionally contains no bindings.